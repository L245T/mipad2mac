# Changelog

## 未发布 — 保持 0.4.0

- 更改目标屏幕、旋转或翻转时保留处理方式；软件控制下安全释放旧笔画并更新映射，原生模式不接管。35 项测试通过，已在应用验证换屏、旋转、两种翻转和原生模式对照。

- 调整侧栏选中圆角及系统强调色；右侧滚动内容与标题栏分离，统一分组对比度、间距、右对齐菜单和原生 NSSwitch。

- 通过 HID 和显示器系统通知识别新的平板连接，唯一匹配就绪后自动选屏并接管；成功后发送通知，不新增设备轮询。手动暂停在同一次连接中保持，重新连接可再次接管。
- 接入状态单元测试通过；当前已连接设备的启动接管已验证，实际拔插和通知展示待用户验证。

## 0.4.0 — 2026-09-17

- 新增稳定版 / Beta 更新渠道，默认稳定版并持久保存选择；Beta 同时检查正式与预发布版本，支持 SemVer 后缀，下载指向具体 Release。
- 更新列表分页读取并限制大小；切换取消旧请求，各渠道独立记录自动检查时间，避免旧结果覆盖新渠道。

- 设置界面改用 AppKit 原生分栏、工具栏标题及 SwiftUI 分组表单，统一控制、权限、测试、设置和关于页。
- 固定侧栏，删除折叠箭头和顶部品牌行；导航改用原生单色 SF Symbols。
- 增加侧栏透明磨砂开关，压缩冗长说明并使用问号说明；关于页图标与文字并排。
- 更新 README、UI 规范与发布说明，明确当前功能和历史实验的边界。

## 0.3.1 — 2026-09-17

- 将压力、倾斜输出设置移到控制页，默认开启并持久保存选择；虚拟按键显示未支持状态，原始报文诊断仍在测试页。
- 菜单栏 HID 增加压力与倾斜输出开关，与控制页同步。切换先释放当前笔状态，保持已启用或暂停状态，不需重新启用控制。

## 0.3.0 — 2026-09-17

- 新增可选的实验性数位笔压力、倾斜与接近/离开事件输出；保留普通鼠标控制路径。已在 macOS 27 / Photoshop 2026 验证压感及倾斜旋转，其他环境待验证。
- 实验输出开关位于测试页，仅本次启动有效；切换后需重新启用控制。
- 新增限时、限量的笔接口原始报文采集及导出，包含压力、倾斜、Report ID 和变化位；未知报文只用于诊断，不驱动输入。
- 捏、双击、滑动笔杆尚未在已知笔接口观察到额外控制数据，不映射功能；手指输入仍不支持。
- 测试窗口展示实际收到的数位笔事件，修复标题与状态文字重叠。
- 自动化测试增至 23 项，覆盖采集边界、倾斜映射及静止压力更新。

## 0.2.0 — 2026-09-16

- 应用与安装包统一使用 0.2.0 版本号；关于页保留代码摘要，正式版本更新比较恢复使用数字版本。

- 测试监控与日志增加持久化开关，默认关闭；关闭时停止测试刷新和新增记录，不影响正常笔控制。
- 导出日志默认打开下载文件夹，仍可自由选择位置。

## 2a5641193abf — 2026-09-16

- 使用实际源码及应用资源摘要作为构建标识，不递增小版本号。

- 关于页和 README 增加微信、支付宝赞助区，使用原始收款二维码。
- 日常构建保持版本号；仅在用户要求创建新版本时升版并同步 GitHub。

## 0.1.16 — 2026-09-16

- 事件提交计数、定位结果及光标坐标移至测试页，控制页保留简短状态。
- 新增设置页，提供关闭到菜单栏、保留 Dock、关闭退出三种行为；默认菜单栏运行且隐藏 Dock 图标。
- 菜单栏文字改为 HID，支持重新打开主窗口；新增系统登录项注册、真实状态及批准指引。

## 0.1.15 — 2026-09-16

- 新增测试页，集中提供输入状态、速率测试和平板触控笔测试入口。
- 增加最多 200 条本地测试记录，支持当前状态快照、清空和文本导出，记录测试窗口实际收到的事件。
- 关于页删除本地证书说明，增加 Powered by GPT6-Astra；关于和 README 标明基于 macOS 27 开发，macOS 26 暂未测试。

## 0.1.14 — 2026-09-16

- 控制页增加 macOS 原生处理与 MiPad2Mac 控制的对比说明，包括屏幕映射、点击拖动、共用光标及能力边界。
- 明确暂停并非禁用触控笔，关闭窗口不会停止控制，视频显示不受控制开关影响。

## 0.1.13 — 2026-09-16

- 顶栏改为控制、权限检查、关于；关于页加入应用 Logo。
- 默认选择唯一识别到的 MI DISPLAY；权限齐全且笔接口描述符匹配时自动启用，手动暂停后不重启控制。
- 缺权限时弹出独立申请页；各项授权按钮在已授权时禁用，并支持重新检查。
- 移除手指鼠标通路检测、USB/HID 分段监控及 Windows 采集脚本；明确当前仅支持触控笔输入。
- 新增自动启用状态测试，覆盖授权完成、设备等待、暂停及单次尝试。

## 0.1.12 — 2026-09-16

- 增加首次使用指引和独立权限检查窗口，保留两个系统设置入口。
- 主窗口改为原生控制、诊断、关于与更新分页，支持缩放及滚动。
- 菜单栏增加控制状态、启用/暂停、目标显示器及指引入口。
- 展示作者力利欧 @L245T；接入 GitHub 正式版本检查，自动检查默认关闭，不自动下载或安装。
- 增加 DMG 构建和校验，沿用已有签名身份。
- 17 项测试通过；已检查指引、权限、主窗口及真实无 Release 提示，验证 DMG 挂载和包内签名。
- 手指输入与系统 Widget 未实现；本地自签仍未公证。

## 0.1.11 — 2026-09-16

- 增加独立 USB/HID 分段监控窗口，覆盖指定平板的全部 HID 输入接口。
- 记录描述符、字段、打开结果及分阶段原始报文，支持导出 JSON，限制采集时间和容量。
- 监控期间暂停桥接，明确 HID 输入与完整 USB 总线抓包的区别。
- 手指输入原因仍需用户完成实机分段操作后分析，不宣称修复。

## 0.1.10 — 2026-09-16

- 保留无品牌标志的橙色平板与白色笔迹，电脑改为带刘海的笔记本。
- 笔记本屏幕增加简洁输入画布和橙色笔迹，与平板形成输入回传的视觉呼应。

## 0.1.9 — 2026-09-16

- 图标去掉 Apple 和 mi 标志，以通用显示器替代 iMac 造型。
- 保留橙色平板、笔与连接线，简化细节并加粗轮廓，使用原创白色输入轨迹。
- 提供未裁切的 SVG 源稿；应用继续使用兼容旧版 macOS 的 ICNS，未迁移 Icon Composer。

## 0.1.8 — 2026-09-16

- 平板屏幕改为橙色，mi 标志改白并放大 45%，苹果标志放大 55%。
- ICNS 使用带透明外边缘的白色圆角底，修复 Dock 图标直角外形。
- 用户确认 v0.1.7 图标已显示。

## 0.1.7 — 2026-09-16

- 显式声明 ICNS 扩展名，启动时直接从应用资源加载 Dock 图标。
- 构建替换后刷新本应用的 Launch Services 注册及包时间，避免继续沿用旧图标元数据。

## 0.1.6 — 2026-09-16

- 应用使用 v5 SVG 复刻 Logo，构建自动生成 16–1024 像素的 macOS ICNS 图标。
- 图标资源在固定证书签名前加入应用包。

## 0.1.5 — 2026-09-16

- 新增 15 秒输入速率测试按钮，保留接收、有效坐标、输出速率及最长回调间隔。
- 分别统计坐标变化与重置报文，无需启用控制即可测量输入。
- 新增统计窗口边界、重复坐标、重置及无输入测试。

## 0.1.4 — 2026-09-16

- 将控制权限和输入监控设置按钮并排显示；请求权限后主动打开对应设置页，已授权时也可打开。
- 速率明确标注“只读”，不是采样率设置。
- 用户确认 v0.1.3 离笔跳边缘已修复，单击正常；快速轨迹细节及手指回传仍未解决。

## 0.1.3 — 2026-09-16

- 过滤已捕获的精确离笔重置报文，抬起仍沿用接触位置，后续重置不再移动光标；正常悬停继续映射。该精确报文若出现在真实角落悬停也会被忽略，角落接触不受影响。
- 报文十六进制格式化移出逐包回调，仅在界面刷新时进行。
- 增加接收报文/秒、提交事件/秒和重置报文计数，区分界面刷新与实际输入速率；未宣称提高设备采样率。
- 用户确认 v0.1.2 单击及启用后的跨屏点击有效；离笔跳边缘、快速移动细节及手指输入仍待验证。
- 新增重复离笔、恢复悬停及角落接触回归测试。

## 0.1.2 — 2026-09-16

- macOS 27 权限名称改为“设备控制和数据访问”；分别检查输入监控与鼠标事件发送权限。
- 控制启用条件使用 CGPreflightPostEventAccess，新增对应请求入口。
- 更正事件“已发送/定位正常”为“已提交/定位调用结果”，不再把 API 调用成功当作端到端验收。
- 新增平板测试页：区分桥接与系统原生事件，显示真正到达窗口的按下、抬起、完整轻点。
- 显示器尺寸明确标为逻辑尺寸，修正手指检测范围与取消状态文案。
- HID 监听及界面定时器使用 common run-loop modes，避免菜单或控件跟踪时暂停处理。
- 鼠标通路诊断先验证字段存在，再订阅鼠标字段；Windows 侧提供只读设备清单脚本。
- 构建可选择已有签名证书（MIPAD_SIGN_IDENTITY），默认仍为临时签名；未自动创建证书或改变权限。

## 0.1.1 — 2026-09-16

- 控制时独占已知笔接口，暂停时恢复系统处理，避免事件竞争。
- 全局坐标显式定位、独立鼠标事件状态。
- 抬笔沿用接触位置；轻微抖动不触发拖动。
- 修复重新连接的设备管理生命周期，显示控制状态及按下/抬起发送计数。
- 新增 15 秒手指回传诊断，只查看复合接口的鼠标字段。
- 新增轻点及拖动抬起回归测试，共 10 项通过；用户实机复测待完成。

## 0.1.0 — 2026-09-16

- 初始原生 macOS 应用与 15 秒只读探测入口。
- 根据实机 `2717:2d05` 数位笔描述符实现严格报文解析。
- 屏幕选择、旋转/翻转、鼠标点击与拖动、双击和安全释放。
- 实时诊断、权限提示、菜单栏暂停，以及协议/映射/状态测试。
- 实验版本：手指回传、真实鼠标控制、多点触控均不能仅以编译及合成测试判定通过。
